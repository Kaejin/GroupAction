<div class="form-box">
  <%= simple_form_for [@fundraiser, @event, @event_update] do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
    <%= f.input :title %>
    <%= f.input :message %>
    <%= f.submit %>
  <% end %>
</div>
