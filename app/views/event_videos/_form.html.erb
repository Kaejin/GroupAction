<div class="form-box">
  <%= simple_form_for [@fundraiser, @event, @event_video] do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
    <%= f.input :title %>
    <%= f.input :link, label: 'Youtube Link' %>
    <%= f.submit %>
  <% end %>
</div>
