<div class="ga-navbar">

  <div class="nav-logo">
    <%= link_to root_path, class: "small-nav-logo" do %>
      <%= image_tag "dark-globe-logo.png" %>
    <% end %>
  </div>

  <!-- notifications tab -->
  <% if user_signed_in? %>
    <% if current_user.admin == true && User.where(is_charity: true, verified: false).count > 0 %>
      <div>
        <p> There are <%= User.where(is_charity: true, verified: false).count %> users to verify </p>
      </div>
    <% end %>
  <% end %>

  <div class="ga-nav-right">
    <% if !user_signed_in? %>
      <div class="sign-up-btn">
        <%= link_to t(".sign_in", default: "Sign up as a Charity"), new_user_registration_path(is_charity: true), class: "ga-link btn text-center" %>
      </div>
    <% elsif current_user.is_charity? && current_user.verified? %>
      <div class="ga-link">
        <%= link_to "Create a fundraiser", new_fundraiser_path, class: "" %>
      </div>
    <% else %>
      <div class="ga-link">
        <p>thing 1</p>
      </div>
    <% end %>

    <% if !user_signed_in? %>
      <div class="sign-up-btn">
        <%= link_to t(".sign_in", default: "Sign up as a User"), new_user_registration_path(is_charity: false), class: "ga-link btn text-center" %>
      </div>
    <% else %>
      <div class="ga-link no-wrap">
        <%= link_to "log out", destroy_user_session_path, method: :delete, class: "ga-link" %>
      </div>
    <% end %>

      <% if !user_signed_in? %>
        <%= link_to t(".sign_in", default: "Log in"), new_user_session_path, class: "ga-link btn text-center" %>
      <% else %>

      <% end %>
    </div>

</div>
